less ~/Dropbox/CreeFST/Wolvengrey/Wolvengrey.csv |
gawk 'BEGIN { FS="\t"; }
NR>=2 && $3 ~ /^NA-1/ { sub("-$","",$15);
    if($1!=$15)
      printf "%s:%s", $1, $15;
    else
      printf "%s", $1;
    printf " \"%s\" ;", $4;
    if(!($10=="" && $11==""))
      printf " ! %s %s", $10, $11;
    if($15=="")
      printf " ! NOSTEM";
    printf "\n"; }'


